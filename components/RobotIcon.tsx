import styles from './RobotIcon.module.css';

const RobotIcon = () => {
  // Component content
  return (
    <span className={ styles.container }>
      🤖
    </span>
  );
};

export default RobotIcon;
